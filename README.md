# Автомобильный маркетплейс

Полнофункциональное веб-приложение для размещения объявлений о продаже автомобилей с системой модерации и общения между пользователями.

## 🐳 Быстрый запуск с Docker

Для быстрого запуска проекта с помощью Docker см. [DOCKER.md](./DOCKER.md)

**Краткая инструкция:**
```bash
# 1. Создайте .env файл
cp .env.example .env

# 2. Отредактируйте .env (установите пароли и секреты)

# 3. Запустите проект
docker-compose up -d
```

После запуска:
- Frontend: http://localhost
- Backend API: http://localhost:3000

## Требования к системе

### Необходимое ПО:
1. **Node.js** версии 18.0 или выше (рекомендуется 22+)
2. **MySQL** версии 8.0 или выше
3. **npm** (устанавливается вместе с Node.js)

## Установка и настройка

### Шаг 1: Установка Node.js

#### Windows:
1. Скачайте установщик с [официального сайта Node.js](https://nodejs.org/)
2. Выберите LTS версию (Long Term Support)
3. Запустите установщик и следуйте инструкциям
4. Проверьте установку в командной строке:
   ```bash
   node --version
   npm --version
   ```

#### Linux (Ubuntu/Debian):
```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### macOS:
```bash
brew install node
```

### Шаг 2: Установка MySQL

#### Windows:
1. Скачайте MySQL Community Server с [официального сайта](https://dev.mysql.com/downloads/mysql/)
2. Запустите установщик MySQL Installer
3. Выберите "Developer Default" и завершите установку
4. Запомните пароль root пользователя, он понадобится позже

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install mysql-server
sudo mysql_secure_installation
```

#### macOS:
```bash
brew install mysql
brew services start mysql
```

### Шаг 3: Настройка базы данных

1. Запустите MySQL и войдите как root:
   ```bash
   mysql -u root -p
   ```

2. Выполните SQL скрипт для создания базы данных:
   ```bash
   mysql -u root -p < DB.sql
   ```

   Или в MySQL консоли:
   ```sql
   source DB.sql;
   ```

3. Проверьте, что база данных создана:
   ```sql
   SHOW DATABASES;
   USE car_marketplace;
   SHOW TABLES;
   ```

### Шаг 4: Установка зависимостей backend

1. Перейдите в директорию backend:
   ```bash
   cd backend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

### Шаг 5: Настройка переменных окружения

1. Создайте файл `.env` в директории `backend/`:

   ```bash
   cd backend
   copy NUL .env  # Windows
   # или
   touch .env     # Linux/macOS
   ```

2. Откройте файл `.env` и добавьте следующие переменные:

   ```env
   # База данных
   DB_HOST=localhost
   DB_PORT=3306
   DB_NAME=car_marketplace
   DB_USER=root
   DB_PASSWORD=ваш_пароль_mysql

   # JWT секретный ключ (генерируйте случайную строку)
   JWT_SECRET=ваш_секретный_ключ_для_jwt_токенов_минимум_32_символа

   # Порт сервера
   PORT=3000

   # Окружение
   NODE_ENV=development
   ```

   **Важно:**
   - Замените `ваш_пароль_mysql` на ваш пароль от MySQL
   - Замените `JWT_SECRET` на случайную строку минимум 32 символа (можно сгенерировать: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)

### Шаг 6: Запуск приложения

#### Запуск backend сервера:

1. В директории `backend/` выполните:
   ```bash
   npm start
   ```

   Или для разработки с автоматической перезагрузкой:
   ```bash
   npm run dev
   ```

2. Если все настроено правильно, вы увидите:
   ```
   ✅ Подключение к базе данных установлено успешно.
   🚀 Сервер запущен на http://localhost:3000
   ```

#### Запуск frontend:

1. Откройте файл `frontend/index.html` в браузере
   
   Или используйте простой HTTP сервер (для корректной работы CORS):

   **Python (если установлен):**
   ```bash
   cd frontend
   python -m http.server 8080
   ```
   Затем откройте: http://localhost:8080

   **Node.js http-server:**
   ```bash
   npm install -g http-server
   cd frontend
   http-server -p 8080
   ```

   **Или используйте Live Server в VS Code:**
   - Установите расширение "Live Server"
   - Правый клик на `index.html` → "Open with Live Server"

## Проверка работы

1. Откройте браузер и перейдите на `http://localhost:8080` (или другой порт фронтенда)
2. Попробуйте зарегистрироваться: нажмите "Регистрация"
3. После регистрации выполните вход
4. Создайте объявление (требуется авторизация)
5. Проверьте список объявлений на главной странице

## Структура проекта

```
PiWebKursach/
├── backend/              # Backend приложение
│   ├── config/          # Конфигурация БД
│   ├── controllers/     # Контроллеры
│   ├── middleware/      # Middleware
│   ├── models/          # Sequelize модели
│   ├── routes/          # Маршруты API
│   ├── utils/           # Утилиты
│   ├── server.js        # Точка входа
│   └── package.json     # Зависимости
├── frontend/            # Frontend приложение
│   ├── css/            # Стили
│   ├── js/             # JavaScript модули
│   ├── images/         # Изображения
│   └── *.html          # HTML страницы
├── DB.sql              # SQL скрипт создания БД
└── README.md           # Этот файл
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Объявления
- `GET /api/listings` - Список объявлений
- `GET /api/listings/:id` - Детали объявления
- `POST /api/listings` - Создать объявление
- `PUT /api/listings/:id` - Обновить объявление
- `DELETE /api/listings/:id` - Удалить объявление

### Комментарии
- `GET /api/comments/listing/:id` - Комментарии объявления
- `POST /api/comments` - Добавить комментарий
- `DELETE /api/comments/:id` - Удалить комментарий

### Избранное
- `GET /api/favorites` - Список избранного
- `POST /api/favorites` - Добавить в избранное
- `DELETE /api/favorites/:id` - Удалить из избранного

### Сообщения
- `GET /api/messages/conversations` - Список бесед
- `GET /api/messages/conversation/:userId` - Беседа с пользователем
- `POST /api/messages` - Отправить сообщение

### Модерация
- `GET /api/moderation/pending` - Непроверенные объявления
- `POST /api/moderation/listing/:id/approve` - Одобрить
- `POST /api/moderation/listing/:id/reject` - Отклонить

## Решение проблем

### Ошибка подключения к БД
- Проверьте, что MySQL запущен
- Проверьте правильность данных в `.env` файле
- Убедитесь, что база данных `car_marketplace` создана

### Ошибка "Port already in use"
- Измените `PORT` в `.env` файле
- Или завершите процесс, использующий порт:
  ```bash
  # Windows
  netstat -ano | findstr :3000
  taskkill /PID <PID> /F
  
  # Linux/macOS
  lsof -ti:3000 | xargs kill
  ```

### CORS ошибки
- Убедитесь, что backend запущен на порту 3000
- Проверьте, что frontend обращается к правильному URL API

### Проблемы с JWT токенами
- Убедитесь, что `JWT_SECRET` в `.env` установлен и достаточно длинный (минимум 32 символа)
- Очистите localStorage в браузере и перелогиньтесь

## Дополнительная информация

- **Порты по умолчанию:**
  - Backend: `3000`
  - Frontend (если используете сервер): `8080`
  - MySQL: `3306`

- **Тестовые данные:**
  - После выполнения `DB.sql` в базе уже есть тестовые пользователи
  - Проверьте таблицу `Users` для получения учетных данных

## Технологии

- **Backend:** Node.js, Express.js, Sequelize ORM
- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **База данных:** MySQL 8.0
- **Аутентификация:** JWT (JSON Web Tokens)
- **Безопасность:** bcrypt, Helmet, CORS, express-rate-limit

## Лицензия

Проект создан в учебных целях.

